apiVersion: v1
kind: Service
metadata:
  name: student-performance
spec:
  type: NodePort
  selector:
    app: student-performance
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
  